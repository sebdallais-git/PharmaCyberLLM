{
  "name": "PharmaLLM - Knowledge Gap Auto-Fill v2 (with Resolution Check)",
  "nodes": [
    {
      "parameters": {
        "path": "knowledge-gap",
        "httpMethod": "POST",
        "responseMode": "onReceived",
        "options": {}
      },
      "id": "a1b2c3d4-0001-4000-8000-000000000001",
      "name": "Knowledge Gap Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        240,
        340
      ],
      "webhookId": "knowledge-gap",
      "notes": "Receives POST from PharmaLLM gap-detector.\nExpects: gap_id, original_query, search_topic, timestamp, gemma_response, reason"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://localhost:3000/api/llm/complete",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: 'gemma2:9b', prompt: 'Generate 3 different web search queries to find information about: ' + $json.search_topic + '\\nOriginal question was: ' + $json.original_query + '\\nReturn ONLY a JSON array of 3 search query strings, nothing else. Example: [\"query 1\", \"query 2\", \"query 3\"]', stream: false }) }}",
        "options": {
          "timeout": 60000
        },
        "sendHeaders": true,
        "specifyHeaders": "keypair",
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{ $env.PHARMALLM_API_TOKEN }}"
            }
          ]
        }
      },
      "id": "a1b2c3d4-0002-4000-8000-000000000002",
      "name": "Generate Search Queries (Ollama)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        480,
        340
      ],
      "notes": "Calls Ollama to generate 3 search queries based on the knowledge gap topic.",
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "// Parse Ollama response to extract 3 search queries\nconst ollamaResponse = $input.first().json.response || '';\nconst searchTopic = $('Knowledge Gap Webhook').first().json.search_topic;\nconst originalQuery = $('Knowledge Gap Webhook').first().json.original_query;\n\nlet queries = [];\n\ntry {\n  // Try to parse JSON array from response\n  const match = ollamaResponse.match(/\\[[\\s\\S]*?\\]/);\n  if (match) {\n    queries = JSON.parse(match[0]);\n  }\n} catch (e) {\n  // Fallback: split by newlines and clean up\n  queries = ollamaResponse.split('\\n').filter(l => l.trim().length > 5).slice(0, 3);\n}\n\n// Ensure we have at least 1 query\nif (queries.length === 0) {\n  queries = [searchTopic];\n}\n\n// Limit to 3 queries\nqueries = queries.slice(0, 3);\n\n// Output one item per query\nreturn queries.map((q, i) => ({\n  json: {\n    query: String(q).replace(/^\"|\"$/g, ''),\n    query_index: i,\n    search_topic: searchTopic,\n    original_query: originalQuery,\n    total_queries: queries.length\n  }\n}));"
      },
      "id": "a1b2c3d4-0003-4000-8000-000000000003",
      "name": "Parse Search Queries",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        720,
        340
      ],
      "notes": "Parses Ollama response into individual query items (up to 3)."
    },
    {
      "parameters": {
        "method": "GET",
        "url": "=http://localhost:8888/search",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "q",
              "value": "={{ $json.query }}"
            },
            {
              "name": "format",
              "value": "json"
            },
            {
              "name": "pageno",
              "value": "1"
            }
          ]
        },
        "options": {
          "timeout": 30000
        }
      },
      "id": "a1b2c3d4-0004-4000-8000-000000000004",
      "name": "Search SearXNG",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        960,
        340
      ],
      "notes": "Searches SearXNG for each query. Runs once per query (up to 3 times).",
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "// Collect results from all SearXNG searches, deduplicate by URL\nconst items = $input.all();\nconst searchTopic = $('Knowledge Gap Webhook').first().json.search_topic;\nconst originalQuery = $('Knowledge Gap Webhook').first().json.original_query;\n\nconst seenUrls = new Set();\nconst results = [];\n\nfor (const item of items) {\n  const searchResults = item.json.results || [];\n  // Take top 3 results per query\n  const top3 = searchResults.slice(0, 3);\n\n  for (const r of top3) {\n    const url = r.url;\n    if (!url || seenUrls.has(url)) continue;\n    // Skip PDFs, images, videos\n    if (/\\.(pdf|jpg|png|gif|mp4|avi)$/i.test(url)) continue;\n    seenUrls.add(url);\n    results.push({\n      json: {\n        url: url,\n        title: r.title || '',\n        search_topic: searchTopic,\n        original_query: originalQuery,\n        total_urls: 0\n      }\n    });\n  }\n}\n\n// Set total count and limit to 9\nconst limited = results.slice(0, 9);\nlimited.forEach(r => r.json.total_urls = limited.length);\n\nif (limited.length === 0) {\n  return [{ json: { error: 'No search results found', search_topic: searchTopic } }];\n}\n\nreturn limited;"
      },
      "id": "a1b2c3d4-0005-4000-8000-000000000005",
      "name": "Deduplicate Results",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1200,
        340
      ],
      "notes": "Collects top 3 results per query, deduplicates by URL. Outputs up to 9 unique URLs."
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $json.url }}",
        "options": {
          "timeout": 30000,
          "response": {
            "response": {
              "responseFormat": "text"
            }
          }
        }
      },
      "id": "a1b2c3d4-0006-4000-8000-000000000006",
      "name": "Fetch Page Content",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1440,
        340
      ],
      "notes": "Fetches each page as text. ContinueOnFail so one bad URL doesn't stop the flow.",
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "// Truncate page content to 8000 chars, strip HTML, filter failed fetches\nconst items = $input.all();\nconst output = [];\n\nfor (let i = 0; i < items.length; i++) {\n  const item = items[i];\n  // Skip failed fetches\n  if (item.json.error || !item.json.data) continue;\n\n  let content = String(item.json.data || '');\n\n  // Strip HTML tags, scripts, styles\n  content = content\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, '')\n    .replace(/<style[\\s\\S]*?<\\/style>/gi, '')\n    .replace(/<nav[\\s\\S]*?<\\/nav>/gi, '')\n    .replace(/<header[\\s\\S]*?<\\/header>/gi, '')\n    .replace(/<footer[\\s\\S]*?<\\/footer>/gi, '')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&[a-z]+;/gi, ' ')\n    .replace(/\\s+/g, ' ')\n    .trim();\n\n  // Skip if too short (likely an error page)\n  if (content.length < 100) continue;\n\n  // Truncate to 8000 chars\n  content = content.slice(0, 8000);\n\n  // Get URL from the Deduplicate node's corresponding item\n  const dedupeItems = $('Deduplicate Results').all();\n  const url = dedupeItems[i] ? dedupeItems[i].json.url : 'unknown';\n  const searchTopic = dedupeItems[i] ? dedupeItems[i].json.search_topic : '';\n\n  output.push({\n    json: {\n      page_content: content,\n      url: url,\n      search_topic: searchTopic,\n      content_length: content.length\n    }\n  });\n}\n\nif (output.length === 0) {\n  return [{ json: { error: 'No pages could be fetched successfully' } }];\n}\n\nreturn output;"
      },
      "id": "a1b2c3d4-0007-4000-8000-000000000007",
      "name": "Truncate & Clean Content",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1680,
        340
      ],
      "notes": "Strips HTML, truncates to 8000 chars, filters out failed fetches and error pages."
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://localhost:3000/api/llm/complete",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: 'gemma2:9b', prompt: 'You are a knowledge extraction assistant. Given this web page content, extract the key facts and information relevant to this topic: ' + $json.search_topic + '\\n\\nWeb content:\\n' + $json.page_content.slice(0, 6000) + '\\n\\nReturn a clean, factual summary in 2-4 paragraphs. If the content is not relevant to the topic, respond with ONLY the word NOT_RELEVANT.', stream: false }) }}",
        "options": {
          "timeout": 120000
        },
        "sendHeaders": true,
        "specifyHeaders": "keypair",
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{ $env.PHARMALLM_API_TOKEN }}"
            }
          ]
        }
      },
      "id": "a1b2c3d4-0008-4000-8000-000000000008",
      "name": "Extract Knowledge (Ollama)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1920,
        340
      ],
      "notes": "Calls Ollama to extract relevant knowledge from each page. Filters irrelevant content.",
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "// Filter out NOT_RELEVANT responses and errors\nconst items = $input.all();\nconst truncateItems = $('Truncate & Clean Content').all();\nconst output = [];\n\nfor (let i = 0; i < items.length; i++) {\n  const item = items[i];\n  const response = String(item.json.response || '').trim();\n\n  // Skip errors and NOT_RELEVANT\n  if (item.json.error) continue;\n  if (response === 'NOT_RELEVANT' || response.startsWith('NOT_RELEVANT')) continue;\n  if (response.length < 50) continue;\n\n  const sourceData = truncateItems[i] || { json: {} };\n\n  output.push({\n    json: {\n      summary: response,\n      url: sourceData.json.url || 'unknown',\n      search_topic: sourceData.json.search_topic || '',\n      summary_length: response.length\n    }\n  });\n}\n\nif (output.length === 0) {\n  return [{ json: { error: 'No relevant content found', relevant_count: 0 } }];\n}\n\nreturn output;"
      },
      "id": "a1b2c3d4-0009-4000-8000-000000000009",
      "name": "Filter Relevant Only",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2160,
        340
      ],
      "notes": "Removes NOT_RELEVANT responses and errors. Only passes through useful summaries."
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://localhost:3000/api/knowledge/ingest-text",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ text: $json.summary, source: 'n8n-gap|' + $json.url }) }}",
        "options": {
          "timeout": 30000
        }
      },
      "id": "a1b2c3d4-0010-4000-8000-000000000010",
      "name": "Store in Knowledge Base",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2400,
        340
      ],
      "notes": "Stores each relevant summary into PharmaLLM knowledge base via the ingest-text API.",
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "// Summarize the entire workflow run\nconst webhookData = $('Knowledge Gap Webhook').first().json;\nconst dedupeItems = $('Deduplicate Results').all();\nconst filteredItems = $('Filter Relevant Only').all();\nconst storedItems = $input.all();\n\nconst totalUrls = dedupeItems.filter(i => !i.json.error).length;\nconst relevantCount = filteredItems.filter(i => !i.json.error).length;\nconst storedCount = storedItems.filter(i => !i.json.error).length;\n\nconst summary = {\n  status: 'completed',\n  gap_id: webhookData.gap_id,\n  search_topic: webhookData.search_topic,\n  original_query: webhookData.original_query,\n  timestamp: new Date().toISOString(),\n  stats: {\n    pages_fetched: totalUrls,\n    relevant_pages: relevantCount,\n    chunks_stored: storedCount\n  }\n};\n\nconsole.log('[N8N Knowledge Gap] ' + JSON.stringify(summary));\n\nreturn [{ json: summary }];"
      },
      "id": "a1b2c3d4-0011-4000-8000-000000000011",
      "name": "Summary & Log",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2640,
        340
      ],
      "notes": "Aggregates stats: pages fetched, relevant pages, chunks stored. Passes gap_id forward."
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://localhost:3000/api/knowledge/gaps/check-resolution",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ gap_id: $json.gap_id, original_query: $json.original_query, search_topic: $json.search_topic }) }}",
        "options": {
          "timeout": 120000
        },
        "sendHeaders": true,
        "specifyHeaders": "keypair",
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{ $env.PHARMALLM_API_TOKEN }}"
            }
          ]
        }
      },
      "id": "a1b2c3d4-0014-4000-8000-000000000014",
      "name": "Check Gap Resolution",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2880,
        340
      ],
      "notes": "After ingesting new content, re-asks the original question through the full RAG pipeline and checks if the gap is now resolved. Updates gap_log.db status to 'resolved' or 'unresolved'.",
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "// Log the resolution result\nconst resolution = $input.first().json;\nconst summary = $('Summary & Log').first().json;\n\nconst result = {\n  status: resolution.resolved ? 'gap_resolved' : 'gap_still_open',\n  gap_id: summary.gap_id,\n  search_topic: summary.search_topic,\n  original_query: summary.original_query,\n  resolved: resolution.resolved || false,\n  confidence_reason: resolution.confidence_reason || 'unknown',\n  ingestion_stats: summary.stats,\n  timestamp: new Date().toISOString()\n};\n\nconsole.log('[N8N Resolution] ' + JSON.stringify(result));\n\nreturn [{ json: result }];"
      },
      "id": "a1b2c3d4-0015-4000-8000-000000000015",
      "name": "Resolution Result Log",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3120,
        340
      ],
      "notes": "Final node: logs whether the knowledge gap was resolved after ingesting new content."
    },
    {
      "parameters": {
        "jsCode": "// SearXNG failed \u2014 log error and stop gracefully\nconst searchTopic = $('Knowledge Gap Webhook').first().json.search_topic;\nconsole.log('[N8N] SearXNG search failed for topic: ' + searchTopic);\nreturn [{\n  json: {\n    status: 'error',\n    stage: 'searxng_search',\n    search_topic: searchTopic,\n    message: 'SearXNG search failed. Check that SearXNG is running on localhost:8888.'\n  }\n}];"
      },
      "id": "a1b2c3d4-0012-4000-8000-000000000012",
      "name": "SearXNG Error Handler",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1200,
        560
      ],
      "notes": "Handles SearXNG failures gracefully \u2014 logs and stops."
    },
    {
      "parameters": {
        "jsCode": "// Ollama query generation failed \u2014 fallback to using search_topic directly\nconst searchTopic = $('Knowledge Gap Webhook').first().json.search_topic;\nconst originalQuery = $('Knowledge Gap Webhook').first().json.original_query;\n\nconsole.log('[N8N] Ollama query generation failed, using search_topic as fallback');\n\nreturn [{\n  json: {\n    query: searchTopic,\n    query_index: 0,\n    search_topic: searchTopic,\n    original_query: originalQuery,\n    total_queries: 1\n  }\n}];"
      },
      "id": "a1b2c3d4-0013-4000-8000-000000000013",
      "name": "Ollama Query Error Handler",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        720,
        560
      ],
      "notes": "If Ollama fails to generate queries, falls back to using the search_topic directly."
    }
  ],
  "connections": {
    "Knowledge Gap Webhook": {
      "main": [
        [
          {
            "node": "Generate Search Queries (Ollama)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Generate Search Queries (Ollama)": {
      "main": [
        [
          {
            "node": "Parse Search Queries",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Ollama Query Error Handler",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse Search Queries": {
      "main": [
        [
          {
            "node": "Search SearXNG",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Ollama Query Error Handler": {
      "main": [
        [
          {
            "node": "Search SearXNG",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Search SearXNG": {
      "main": [
        [
          {
            "node": "Deduplicate Results",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "SearXNG Error Handler",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Deduplicate Results": {
      "main": [
        [
          {
            "node": "Fetch Page Content",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Page Content": {
      "main": [
        [
          {
            "node": "Truncate & Clean Content",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Truncate & Clean Content",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Truncate & Clean Content": {
      "main": [
        [
          {
            "node": "Extract Knowledge (Ollama)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extract Knowledge (Ollama)": {
      "main": [
        [
          {
            "node": "Filter Relevant Only",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Filter Relevant Only",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Filter Relevant Only": {
      "main": [
        [
          {
            "node": "Store in Knowledge Base",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Store in Knowledge Base": {
      "main": [
        [
          {
            "node": "Summary & Log",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Summary & Log",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Summary & Log": {
      "main": [
        [
          {
            "node": "Check Gap Resolution",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check Gap Resolution": {
      "main": [
        [
          {
            "node": "Resolution Result Log",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Resolution Result Log",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "callerPolicy": "workflowsFromSameOwner"
  },
  "staticData": null,
  "tags": [
    {
      "name": "PharmaLLM"
    },
    {
      "name": "knowledge-gap"
    },
    {
      "name": "v2-resolution"
    }
  ],
  "triggerCount": 1,
  "updatedAt": "2026-03-09T00:00:00.000Z",
  "versionId": "2",
  "id": "knowledge-gap-autofill-v2"
}
